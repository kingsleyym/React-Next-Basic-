{
  "name": "@repo/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "//": "Provider SDKs (firebase, @supabase/supabase-js) are intentionally NOT declared here. The adapters under src/**/adapters are excluded from the build; install the SDK in the consuming app only when you switch provider. See docs/BACKEND-PROVIDERS.md."
}
